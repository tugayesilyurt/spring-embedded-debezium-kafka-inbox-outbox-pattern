server:
  port: 7002

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/saga
    username: saga
    password: saga


kafka:
  bootstrapAddress: localhost:29092

